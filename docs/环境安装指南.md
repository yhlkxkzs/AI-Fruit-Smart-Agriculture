# 任务1: 水果种类识别 - Conda环境安装指南

## 📋 前提条件

您已经创建了conda环境 `fruit_classification`。

---

## 🚀 安装步骤

### 步骤1: 激活conda环境

```bash
conda activate fruit_classification
```

### 步骤2: 升级pip

```bash
pip install --upgrade pip
```

### 步骤3: 安装PyTorch

**如果使用GPU（推荐）**:
```bash
# CUDA 11.8版本
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 或 CUDA 12.1版本
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

**如果只使用CPU**:
```bash
pip install torch torchvision torchaudio
```

**验证PyTorch安装**:
```bash
python -c "import torch; print(f'PyTorch版本: {torch.__version__}'); print(f'CUDA可用: {torch.cuda.is_available()}')"
```

### 步骤4: 安装YOLOv8

```bash
pip install ultralytics>=8.0.0
```

**验证YOLOv8安装**:
```bash
python -c "from ultralytics import YOLO; print('YOLOv8安装成功')"
```

### 步骤5: 安装图像处理库

```bash
pip install opencv-python>=4.8.0
pip install Pillow>=9.0.0
pip install albumentations>=1.3.0
```

### 步骤6: 安装数据处理库

```bash
pip install numpy>=1.24.0
pip install pandas>=2.0.0
```

### 步骤7: 安装可视化库

```bash
pip install matplotlib>=3.7.0
pip install seaborn>=0.12.0
pip install tqdm>=4.65.0
```

### 步骤8: 安装API框架（可选，用于后续API开发）

```bash
pip install fastapi>=0.100.0
pip install uvicorn[standard]>=0.23.0
pip install python-multipart>=0.0.6
```

### 步骤9: 安装开发工具（可选）

```bash
pip install jupyter>=1.0.0
pip install tensorboard>=2.13.0
```

### 步骤10: 验证环境

```bash
cd /home/yuhanlin/APP/AFSA/task1_fruit_classification
python verify_environment.py
```

---

## 📦 一键安装（推荐）

我已经创建了安装脚本 `install_dependencies.sh`，您可以直接运行：

```bash
cd /home/yuhanlin/APP/AFSA/task1_fruit_classification
bash install_dependencies.sh
```

或者手动执行以下命令：

```bash
# 激活环境
conda activate fruit_classification

# 升级pip
pip install --upgrade pip

# 安装PyTorch（根据您的CUDA版本选择）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118  # GPU版本
# 或
pip install torch torchvision torchaudio  # CPU版本

# 安装YOLOv8
pip install ultralytics>=8.0.0

# 安装图像处理库
pip install opencv-python>=4.8.0 Pillow>=9.0.0 albumentations>=1.3.0

# 安装数据处理库
pip install numpy>=1.24.0 pandas>=2.0.0

# 安装可视化库
pip install matplotlib>=3.7.0 seaborn>=0.12.0 tqdm>=4.65.0

# 安装API框架
pip install fastapi>=0.100.0 uvicorn[standard]>=0.23.0 python-multipart>=0.0.6

# 安装开发工具
pip install jupyter>=1.0.0 tensorboard>=2.13.0

# 验证环境
python verify_environment.py
```

---

## ✅ 验证安装

运行验证脚本：

```bash
cd /home/yuhanlin/APP/AFSA/task1_fruit_classification
python verify_environment.py
```

应该看到：
- ✅ Python版本符合要求
- ✅ PyTorch已安装
- ✅ YOLOv8可以正常导入
- ✅ 所有核心包都已安装

---

## 📝 注意事项

1. **CUDA版本**: 如果使用GPU，请根据您的CUDA版本选择合适的PyTorch版本
2. **网络问题**: 如果pip安装较慢，可以使用国内镜像：
   ```bash
   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple <package_name>
   ```
3. **环境隔离**: 确保在`fruit_classification`环境中安装，不要安装到base环境

---

## 🔧 常见问题

### 问题1: conda activate失败
**解决方案**: 
```bash
source $(conda info --base)/etc/profile.d/conda.sh
conda activate fruit_classification
```

### 问题2: PyTorch安装失败
**解决方案**: 
- 检查CUDA版本: `nvidia-smi`
- 或使用CPU版本: `pip install torch torchvision torchaudio`

### 问题3: ultralytics安装失败
**解决方案**: 
```bash
pip install --upgrade pip
pip install ultralytics -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

**安装完成后，请运行 `python verify_environment.py` 验证环境配置！**
